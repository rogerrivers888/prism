{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "RAILPACK" },
  "deploy": {
    "startCommand": "uv run python -m app.worker.daily",
    "restartPolicyType": "NEVER"
  }
}
